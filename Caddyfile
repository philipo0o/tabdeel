egyptiancyclingobservatory.com, www.egyptiancyclingobservatory.com {
    # Reverse proxy to the frontend
    reverse_proxy frontend:3002

    # Reverse proxy /api to the backend
    # We do NOT use handle_path because the backend routes already include /api
    reverse_proxy /api* backend:3001
}
